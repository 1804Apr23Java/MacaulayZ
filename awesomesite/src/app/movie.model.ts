export class Movie {
    title: string;
    year: number;
    rating: string;

    constructor(title: string, year: number, rating: string) {
        this.title = title;
        this.year = year;
        this.rating = rating;
    }
}